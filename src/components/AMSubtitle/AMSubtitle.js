import React from "react";

import "./AMSubtitle.scss";

const AMSubtitle = () => {
  return (
    <div className="AMSubtitle">
      <p>
        <span className="text-gold">140 M </span>
        people will have access to electricity through
        <span className="text-gold"> Microgrids </span>
        and
        <span className="text-gold"> Minigrids </span>
        by
        <span className="text-gold"> 2040 </span>
        in Africa.
      </p>
    </div>
  );
};

export default AMSubtitle;
